package main
import (
	"fmt"
	"strings"
)

func main() {
	a := "abcdefg"
	fmt.Println(a)
	fmt.Println(a[1:]) // 截取方法与slice/array相同
	// strings 包是一个常见的操作字符串的包， 基本的方法都有
	fmt.Println(strings.Index(c, "wo"))
}